const TaverFactory = artifacts.require("TavernFactory");
const utils = require("./helpers/utils")

contract("TavernFactory" , async (accounts) => {

    // let contractInstace = Tavern
    // beforeEach() {

    // }

    // context("adding user" , )
})
